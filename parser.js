#!/usr/bin/env node
/* Совместимость: `node parser.js [city]` → запускает мульти-город сборщик parser/index.js.
   Архитектура и адаптеры городов — в каталоге parser/. */
require('./parser/index.js');
